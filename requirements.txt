# ═══════════════════════════════════════════════════════════════════════════════
# NeuralBot OMEGA — Complete Dependency Manifest
# All versions pinned. Zero ambiguity. Audit-verified.
# ═══════════════════════════════════════════════════════════════════════════════

# ── Core Async Runtime ────────────────────────────────────────────────────────
winloop==0.1.3 ; sys_platform == 'win32'
uvloop==0.22.1 ; sys_platform != 'win32'
uvicorn[standard]==0.27.0
aiohttp==3.9.3
httpx==0.27.0
websockets==12.0

# ── Web Framework (Backend API) ───────────────────────────────────────────────
fastapi==0.110.0
pydantic==2.6.1

# ── Blockchain / Web3 ────────────────────────────────────────────────────────
web3==6.15.0
eth-abi==4.2.1
eth-account==0.11.0
eth-typing==4.0.0
eth-utils==3.0.0

# ── MEV Protection ───────────────────────────────────────────────────────────
flashbots==1.3.0

# ── Predictive Intelligence (numpy ONLY — no torch/tf) ───────────────────────
numpy==1.26.4

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.27
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9

# ── Auth / Payments ───────────────────────────────────────────────────────────
pyjwt==2.8.0
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
stripe==8.4.0

# ── Observability ─────────────────────────────────────────────────────────────
structlog==24.1.0
prometheus-client==0.20.0

# ── Resilience ────────────────────────────────────────────────────────────────
tenacity==8.2.3

# ── Dev / Config ──────────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Optional: scipy for Savitzky-Golay (PCFTN Layer 3) ───────────────────────
scipy==1.12.0  # uncomment if available; numpy fallback exists

# ── Optional: Fast JSON (engine falls back to stdlib json) ────────────────────
orjson==3.9.12
